% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_filter.R
\name{text_filter}
\alias{text_filter}
\title{Фильтр строк по шаблону}
\usage{
text_filter(x, y)
}
\arguments{
\item{x}{character вектор строк, в которых выполняется поиск.}

\item{y}{character вектор шаблонов (регулярных выражений). Все шаблоны объединяются в одно выражение с помощью OR (\code{|}).}
}
\value{
Логический вектор того же размера, что и \code{x}, где \code{TRUE} означает, что соответствующая строка содержит хотя бы один из шаблонов.
}
\description{
Эта функция проверяет, содержится ли любой из указанных шаблонов в строках векторе \code{x}.
}
\details{
Для поиска используется функция \code{str_detect} из пакета \code{stringr}. Внутри шаблонов с помощью функции \code{paste} создаётся объединённое регулярное выражение, разделённое оператором OR (\code{|}).
Параметр \code{ignore_case = TRUE} делает поиск нечувствительным к регистру.
}
\examples{
# Пример использования:
texts <- c("Apple", "Banana", "Cherry", "apricot")
patterns <- c("a", "ch")
text_filter(texts, patterns)
# Вернёт: TRUE FALSE TRUE TRUE

}
